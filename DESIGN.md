# MetricLens 设计说明

## 目标

日常在 GA4、AppsFlyer、Meta 广告后台看报表时，指标缩写太多记不住。
希望在**看到的地方**就能查，而不是切标签页翻文档。同时这份词库要能持续增长，并分享给组内同学。

## 三条约束决定了架构

1. **知识库是唯一数据源** —— 词条写在 Markdown 里（人读的格式），编译成 JSON（机器读的格式）。
   不维护两份内容，避免不一致。
2. **私有内容绝不外泄** —— 知识库里混有内部项目的教训。用 `<!-- private -->` 标记 + 构建时
   关键词兜底扫描，双保险。目录也物理隔离：私有知识库和公开扩展是两个仓库。
3. **同学要能一眼看懂代码** —— 决定了不引框架、不用构建工具、不压缩。原生 JS + 一个 Python 脚本。

## 数据流

```
/Users/lcx/knowledge/*.md          私有仓库，词条原文
        │
        │  build.py
        │    ├─ 解析 ## 标题 → 词条
        │    ├─ 抽取 一句话 / 公式 / 相关词
        │    ├─ 生成别名（标题斜杠拆分 + 副标题 + 英文全称 + aliases.json）
        │    ├─ 剔除 <!-- private --> 段落
        │    └─ 扫描项目代号兜底，命中则中止构建
        ▼
   data/terms.json                 公开仓库，构建产物
        ├──> extension/data/       打包进扩展（离线兜底）
        └──> GitHub raw            background.js 每 6 小时拉一次
```

## 关键设计取舍

**为什么词条数据放 storage 而不是每次 fetch**
content script 要在每个页面初始化，从 `chrome.storage.local` 读比 fetch 扩展资源快，
也让远程更新能立即对所有已打开的标签页生效（通过 `storage.onChanged` 热替换词典）。
storage 为空时回退到扩展内置的 JSON，保证第一次安装即可用。

**为什么卡片用 Shadow DOM**
扩展会跑在任何页面上，宿主页面的 CSS 五花八门。Shadow DOM + `all:initial`
是唯一能保证卡片长相一致的办法，否则总有页面把卡片的字体或颜色搞坏。

**为什么自己写 Markdown 渲染器**
词条正文有表格、代码块、双链。引 marked 之类的库会带来 MV3 CSP 的麻烦和几十 KB 体积，
而实际用到的语法只有七种。150 行手写渲染器覆盖全部场景，同学也能读懂。
所有输出都经过 HTML 转义。

**词匹配的两个坑**
- *词边界*：`CPI` 不能命中 `CPIA`、`UA` 不能命中 `UAT`。纯 ASCII 别名做前后字符检查，
  中文别名不需要（中文没有词边界）。
- *长别名优先*：`LT30` 必须优先于 `LT`。别名按长度降序扫描 + 区间占用表，
  短别名不能覆盖已被长别名占用的位置。

**为什么未命中时静默**
选中文本弹窗如果命中率低就是骚扰。只有确实查到词条才弹卡片，
让用户正常复制数字时完全无感。

**自动高亮为什么默认关**
报表页面上指标名词密集，全加下划线会让页面很花。做成可选开关，
且用 300 个节点的上限和 600ms 防抖，避免拖慢重型 SPA。

## 已知限制

- `all_frames: false` —— iframe 内的内容划不了词。主流后台的主界面都不在 iframe 里，
  等真遇到再说。
- 自动高亮对虚拟滚动列表（表格行复用 DOM）可能重复处理，这也是它默认关闭的原因之一。
- 远程更新只认整份 JSON 替换，没有增量。词库到几千条之前都不是问题（当前 46 KB）。

## 测试策略

`jsc`（macOS 自带 JavaScriptCore）跑纯逻辑单测，不需要装 Node。
覆盖词典匹配的全部边界情况和 Markdown 渲染，外加"全部词条整篇渲染不抛异常"的冒烟测试。
UI 部分靠 `test/demo.html` 在真浏览器里人工验证。
